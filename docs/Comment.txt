[V1.3.2 STU-Net 2026-06-07] Two-stage STU-Net-B cascade (Ds539 5-class anatomy -> per-anatomy 3ch ROI -> Ds538 ABBC -> core-seed watershed -> official ranges, femur 151-200), leakage-free case-grouped split. V1.3.2 ROOT-CAUSE FIX for the prior ~0 GC scores: nnUNet 2.5.1's initialize_from_trained_model_folder does NOT load weights into predictor.network (it defers to perform_actual_prediction, which our custom predict path bypasses), so both stages ran with RANDOM init -> speckle anatomy (tens of thousands of CCs) -> ~0. build_predictor now loads the checkpoint into the network explicitly (version-independent). Reproduced locally on 2.5.1 (w0sum random 82.9, Ds539 6812 CCs) and verified fixed (w0sum 104.03, 2 CCs, Femur anatomy-Dice 0.98). Retains V1.3 robust routing + self-diagnostic logging (now incl. w0sum). Stage-2-ROI proxy (oracle anatomy): Dice 0.799, F1 0.919, HD95 18.9mm. <T4/10min.
