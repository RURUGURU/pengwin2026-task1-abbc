[V1.3.3 STU-Net 2026-06-07] Two-stage STU-Net-B cascade (Ds539 5-class anatomy -> per-anatomy 3ch ROI -> Ds538 ABBC -> core-seed watershed -> official ranges, femur 151-200), leakage-free case-grouped split. Builds on the V1.3.2 root-cause fix (nnUNet 2.5.1 was running random weights). V1.3.3 = inference-only decode/cleanup, model UNCHANGED: (1) DEC-2 fix for under-segmentation — the deploy decoder's non-Sacrum size-ratio merge (0.05) was collapsing genuine 1-11 cm^3 fracture fragments (it had silently diverged from the validated offline eval, which uses 0.0); disabling it recovers real fragments (local instance-recall proxy 0.46->0.51, F1 0.50->0.55, split_error stays 0, precision held; Sacrum keeps its 0.10 precision guard). (2) ROB-3 — removed a speckle-era "bbox>50%" band-aid that fired on clean large anatomy in tight FOV and swapped the soft probability ROI for a hard bone mask (OOD decode). (3) Removed ~120 lines of dead/legacy code. A multi-agent audit drove this; a confidence-based routing guard for the cross-group hallucination was prototyped and REJECTED on evidence (Ds539 is confidently-wrong: real and phantom bones both score 0.97-0.99, so it is a model-level issue for future retraining, not an inference threshold). <T4/10min.
