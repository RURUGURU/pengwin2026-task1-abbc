V0.3.3 = V0.3.1 four-layer anatomy-conditioned pipeline (bone-skeleton + Ds532 argmax + bbox sanity + time budget) + Ds532 mask morphology cleanup (binary_opening + small-CC drop, MIN_DS532_CC_VOXELS=500) + bone-skeleton fallback after bbox-sanity fail. Held-out (n=5, out-of-fold0, V0.3.1 baseline): Fracture Dice 0.62, Instance F1 0.81 (+0.12 vs V0.2), HD95 20.6 mm (-6.1 mm). A/B (V0.3.3 vs V0.3.1) on a 3-case small-FOV pelvic slice: zero cohort delta (no regression); morphology fired on case 162 (5 sparse CCs dropped on Sacrum/RightHip) but dominant ROI unchanged. V0.3.3 is a safety-net change against a known Ds532 sparse-outlier failure mode. V0.2 GC timeout fixed. Femur (151-200) intentionally not modeled in V0.x; planned for V1. Hard-trio (003/011/017, fold0 train) is training-set fit, not held-out.
