[V0.3.4 HEADLINE 2026-05-30] V0.3.4 4-layer + Ds532 morphology + bone-skeleton routing: cumulative V0.3.x stack = L1 bone-skeleton HU mask + L2 Ds532 per-anatomy argmax + L3 bbox sanity + L4 time budget, with Ds532 morphology cleanup and bone-skeleton fallback, plus bone-skeleton-aware pelvic/femur routing (classify_pelvic_femur_v2) that fixes small-FOV spacing_z=0.80 misroutes (e.g. case 189) while staying bit-identical to V0.3.3 elsewhere. Held-out (n=5): Dice 0.62, F1 0.81, HD95 20.6 mm.

V0.3.4 = V0.3.3 robustness stack (L1 bone-skeleton + L2 Ds532 argmax + L3 bbox sanity + L4 time budget + Ds532 morphology cleanup + bone-skeleton fallback) + bone-skeleton-aware pelvic/femur routing fix (classify_pelvic_femur_v2). The V0.3.1 spacing-only router misrouted small-FOV pelvic cases with spacing_z=0.80 (e.g. case 189) to the femur branch -> zero output; V0.3.4 runs the L1 bone HU mask before the spacing rule and force-routes to pelvic when >=2 of {Sacrum, LeftHip, RightHip} are recovered, threading the masks through into L1 to stay bit-identical to V0.3.3 on cases the spacing rule already routed correctly. A/B (V0.3.4 vs V0.3.3) on the 3-case small-FOV slice (162/163/189): case 189 rescued from femur to pelvic (zero -> 11.2M nonzero voxels, full V291 ABBC pipeline runs); cases 162/163 byte-identical. Cohort headline delta is 0.000 on Fracture Dice and Instance F1: the routing fix works end-to-end on case 189, but on this small-FOV slice the V291 decoder paints fragments substantially larger than GT (IoU 0.06-0.08, below the 0.10 match threshold) so per-anatomy argmax matching produces no matched pred and metrics stay at 0 — a Stage 2 model/decoder accuracy issue, not a routing issue, tracked for V1. V0.3.4 is a strictly additive routing-correctness fix; held-out projection (n=5 out-of-fold0): Fracture Dice 0.62, Instance F1 0.81, HD95 20.6 mm (bit-identical to V0.3.1/V0.3.3 on the held-out cohort because the routing change does not fire there). Femur (151-200) intentionally not modeled in V0.x; planned for V1.
