[model_v3_0 hybrid family router 2026-07-25] Two-stage STU-Net-B cascade for PENGWIN Task 1. Stage A is Dataset539 5-class anatomy STU-Net (Sacrum, LeftHip, RightHip, Femur). Stage B is Dataset538 V308 ABBC+9-affinity STU-Net (13ch = 4 ABBC + 9 affinity), decoded with average-linkage agglomeration (decode_affinity_agglo, AGGLO_T=0.45) and mapped to the official label ranges (1-50 sacrum, 51-100 left hip, 101-150 right hip, 151-200 femur). model_v3_0 keeps the STU-Net weights unchanged and adds a hybrid random-forest target-family router packaged with the model payload as stage1_router/stage1_target_router_fold0.joblib. The RF uses CT/FOV/bone-geometry features to classify the case as pelvic or femur and is primary when confident (|p_femur-0.5| >= 0.15); the organizers' official pelvic/femur rule is a tiebreak only on RF-uncertainty. Stage B then runs only Sacrum+LeftHip+RightHip or only Femur. History: v2.2 = prelim rank 10; v3.2 (official-rule authoritative) regressed to val rank 44 via a 13% misroute; v3.3 hybrid fixed it (val rank 30); TEST phase F1 0.898, ~rank 13. Runtime remains single-GPU, serial Stage A then Stage B, within the T4/10min budget.
